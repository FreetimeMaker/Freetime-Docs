# Getting Started

```bash
npm install -g @neutralinojs/neu
git clone https://github.com/FreetimeMaker/Lumex-Client.git
cd Lumex-Client
neu update
neu run
```

The launcher separates routing, authentication, installation, instances, Java detection, Minecraft launching and settings into modules.
