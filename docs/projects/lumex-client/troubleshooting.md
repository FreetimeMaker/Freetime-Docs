# Troubleshooting

## Java is not detected

Check Java detection and the configured executable/path before Minecraft arguments.

## Minecraft does not start

Treat installation, Java detection, instance metadata and process launch as separate stages.

## Native operation denied

Check `nativeAllowList` in `neutralino.config.json`.
