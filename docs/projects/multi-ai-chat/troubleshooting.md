# Troubleshooting

## Provider request fails

Verify provider, model identifier and API key independently. A valid key can still fail for a retired/unsupported model.

## Plugin incompatible with IDE

Check the IntelliJ Platform target and compatibility before changing Kotlin or networking dependencies.
