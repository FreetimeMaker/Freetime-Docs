# Getting Started

Multi AI Chat is a JetBrains IDE plugin.

```bash
git clone https://github.com/FreetimeMaker/Multi-AI-Chat.git
cd Multi-AI-Chat
./gradlew buildPlugin
```

Provider API keys belong in user/plugin settings and must never be committed.
