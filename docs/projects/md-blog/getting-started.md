# Getting Started

```bash
git clone https://github.com/FreetimeMaker/MD-Blog.git
cd MD-Blog
npm install
npm run dev
```

## Publish a post

Add a Markdown file under `public/blogs`. The filename becomes its slug. Vercel routing maps the homepage, blog listing and posts to Node handlers.
